public-hoist-pattern[]=*
shamefully-hoist=true
node-linker=hoisted
# Or let it be side-by-side but with hoisting to avoid `error NG1010` sometimes in some environments when files are not found.
# Actually shamefully-hoist=true is more common for transition.
